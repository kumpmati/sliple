import { describe, it } from 'vitest';

describe('verifyPuzzleWin', () => {
	it.todo('correctly verifies a valid solution');
	it.todo('returns an error if a move is invalid');
	it.todo("returns an error if the moves don' result in a win");
});
